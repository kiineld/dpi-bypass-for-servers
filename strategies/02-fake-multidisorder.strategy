# Sourced by dpibypass. %TCP% / %UDP% are replaced with your configured ports.
STRATEGY_NAME="Fake + MultiDisorder"
STRATEGY_DESC="Fake packet plus out-of-order resend of segments. Try when 01 fails — beats DPI that reassembles simple splits."
STRATEGY_OPT="--filter-tcp=%TCP% --lua-desync=fake:blob=fake_default_tls:badsum:strategy=1 --lua-desync=multidisorder:strategy=2 --new --filter-udp=%UDP% --filter-l7=quic --lua-desync=fake:blob=fake_default_quic:badsum:strategy=1"
