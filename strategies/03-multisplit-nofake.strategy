# Sourced by dpibypass. %TCP% / %UDP% are replaced with your configured ports.
STRATEGY_NAME="MultiSplit only (no fake)"
STRATEGY_DESC="Splits the handshake without any fake packet. Use where the DPI detects and drops fakes (some TSPU setups), or when fakes cause resets."
STRATEGY_OPT="--filter-tcp=%TCP% --lua-desync=multisplit:strategy=1 --new --filter-udp=%UDP% --filter-l7=quic --lua-desync=fake:blob=fake_default_quic:badsum:strategy=1"
