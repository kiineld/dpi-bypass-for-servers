# Sourced by dpibypass. %TCP% / %UDP% are replaced with your configured ports.
STRATEGY_NAME="MultiDisorder only (no fake)"
STRATEGY_DESC="Out-of-order resend without a fake packet. A quieter variant of 03 for flow-killing DPI that trips on fakes."
STRATEGY_OPT="--filter-tcp=%TCP% --lua-desync=multidisorder:strategy=1 --new --filter-udp=%UDP% --filter-l7=quic --lua-desync=fake:blob=fake_default_quic:badsum:strategy=1"
